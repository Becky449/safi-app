<%= form_with(model: agrovet, local: true, class: "my-4") do |f| %>
  <div class="row">
    <div class="col-md-6">
      <% if agrovet.errors.any? %>
        <div class="alert alert-danger">
          <h2><%= pluralize(agrovet.errors.count, "error") %> prohibited this agrovet from being saved:</h2>
          <ul>
            <% agrovet.errors.full_messages.each do |error| %>
              <li><%= error %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="mb-3">
        <%= f.label :name, class: "form-label" %>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :location, class: "form-label" %>
        <%= f.text_field :location, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :owner_name, class: "form-label" %>
        <%= f.text_field :owner_name, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :owner_phone, class: "form-label" %>
        <%= f.number_field :owner_phone, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :owner_email, class: "form-label" %>
        <%= f.text_field :owner_email, class: "form-control" %>
      </div>
    </div>

    
      <div class="mb-3">
        <%= f.hidden_field :user_id, value: @agrovet.user_id, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.submit "Save", class: "btn btn-danger btn-lg" %>
      </div>
    
  </div>
</div>
<% end %>
