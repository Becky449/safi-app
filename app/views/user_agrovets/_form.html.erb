<%= form_with(model: user_agrovet) do |form| %>
  <% if user_agrovet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_agrovet.errors.count, "error") %> prohibited this user_agrovet from being saved:</h2>

      <ul>
        <% user_agrovet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :agrovet_id, style: "display: block" %>
    <%= form.text_field :agrovet_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
